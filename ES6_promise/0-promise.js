export default function getResponseFromAPI() {
  return new Promise(() => {
    // const succes = true;
    //     if (succes) {
    //         resolve(true);
    //     } else {
    //         reject(false);
    //     }
    // });
  });
}
